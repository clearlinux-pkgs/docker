From ad4c3b902a701c7406416d7d6b0ddaedd1fe804b Mon Sep 17 00:00:00 2001
From: Julio Montes <julio.montes@intel.com>
Date: Mon, 8 Aug 2016 13:40:31 -0500
Subject: [PATCH] add cc-oci-runtime as default runtime

switch to overlay storage driver

Signed-off-by: Julio Montes <julio.montes@intel.com>
---
 contrib/init/systemd/docker.service | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/contrib/init/systemd/docker.service b/contrib/init/systemd/docker.service
index c3f3472..9d67ae4 100644
--- a/contrib/init/systemd/docker.service
+++ b/contrib/init/systemd/docker.service
@@ -9,7 +9,7 @@ Type=notify
 # the default is not to use systemd for cgroups because the delegate issues still
 # exists and systemd currently does not support the cgroup feature set required
 # for containers run by docker
-ExecStart=/usr/bin/dockerd -H fd://
+ExecStart=/usr/bin/dockerd -H fd:// --storage-driver=overlay --add-runtime cor=cc-oci-runtime --default-runtime=cor
 ExecReload=/bin/kill -s HUP $MAINPID
 # Having non-zero Limit*s causes performance problems due to accounting overhead
 # in the kernel. We recommend using cgroups to do container-local accounting.
-- 
2.1.0

